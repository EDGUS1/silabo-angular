import { CompentenciaEspecifica } from './compentencia-especifica';

describe('CompentenciaEspecifica', () => {
  it('should create an instance', () => {
    expect(new CompentenciaEspecifica()).toBeTruthy();
  });
});
